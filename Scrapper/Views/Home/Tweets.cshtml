@model IEnumerable<Scrapper.Models.UserTweets>

<p>
    Tweets
    @*<a asp-action="Create">Create New</a>*@
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @*@Html.DisplayNameFor(model => model.text)*@
                Tweet
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.retweets)*@
                Retweet Counter
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.text)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.retweets)
            </td>
            @*<td>
                <a asp-action="Edit" asp-route-id="@item.UserTweetsId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserTweetsId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserTweetsId">Delete</a>
            </td>*@
        </tr>
}
    </tbody>
</table>
